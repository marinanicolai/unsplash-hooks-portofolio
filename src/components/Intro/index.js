import React from "react";
import { ContainerTitle, Title } from "./Intro.styles";

const Intro = () => {
  return (
    <ContainerTitle>
      <Title>Photo Gallery</Title>
    </ContainerTitle>
  );
};

export default Intro;
